// Clean initial seed data for Heavy Duty

export const DEFAULT_WEEKLY_SCHEDULE = {
  0: { type: 'rest', label: 'Descanso' }, // Domingo
  1: { type: 'rest', label: 'Descanso' }, // Segunda
  2: { type: 'rest', label: 'Descanso' }, // Terça
  3: { type: 'rest', label: 'Descanso' }, // Quarta
  4: { type: 'rest', label: 'Descanso' }, // Quinta
  5: { type: 'rest', label: 'Descanso' }, // Sexta
  6: { type: 'rest', label: 'Descanso' }  // Sábado
};

export const INITIAL_USER = null;

export const INITIAL_ROUTINES = [];

export const INITIAL_GROUPS = [];

export const INITIAL_CHECKINS = [];

export const INITIAL_MESSAGES = [];
